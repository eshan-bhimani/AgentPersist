# AgentPersist
Coding agent with MongoDB-backed memory: vector search for repo conventions, LangGraph checkpointing for crash recovery

A coding agent with persistent memory: retrieves repo conventions via MongoDB Atlas vector search and recovers from crashes mid-task using LangGraph checkpointing.
